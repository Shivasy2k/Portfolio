{
  "name": "portfolio-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static portfolio site. Content lives in /content, builds to /data, deploys via GitHub Pages.",
  "scripts": {
    "build": "node scripts/build.js",
    "serve": "python3 -m http.server 8080",
    "dev": "npm run build && npm run serve"
  },
  "engines": {
    "node": ">=18"
  }
}
